name: Deploy Pipeline

on:
  push:
    branches: [ "main" ]
    # trigger deploy only when pushed to main

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    needs: [] # Assuming it runs after a successful CI job
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        
      - name: Deploy to Vercel/Production
        run: echo "Deploy script goes here (e.g., vercel deploy --prod)"
        
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        
      - name: Build and push Docker image
        run: |
          echo "Docker build and push steps for backend go here"
